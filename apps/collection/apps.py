# Define the application
from django.apps import AppConfig


class AppsConfig(AppConfig):
    name = 'apps.collection'
    verbose_name = "7、Collection Management"  # The navigation bar that appears on the admin home page in the backend.
